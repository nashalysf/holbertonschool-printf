#ifndef _PRINTF_
#define _PRINTF_
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <unistd.h>
#include <stdarg.h>
int _printf(const char *format, ...);
typedef struct prints
{
	char *t;

	int *f(va_list);
} print;
_write(char c);
#endif
