.TH _PRINTF

.SH NAME
 _printf - Custom function to format and print output.

.SH SYNOPSIS
.RS
#include "main.h"

int _printf(const char *format,...);
.RE
.YS

.SH DESCRIPTION
_printf is a custom function that prints arguments according to format and prints the formatted string. To use conversion specifiers add % followed by:

.RS
 c      Print a character.
 s      Print a string.
 i      Print an integer.
 d      Print a decimal.
.RE

.SH EXAMPLES

-To print with multiple arguments:

#include "main.h"


int main()
.IP {
.RS
int age = 32;

char letter = 'J';

_printf("My age is %i and the first letter of my name is %c.", age, letter);
.RE
}

My age is 32 and the first letter of my name is J.

.EE

.SH RETURN VALUE
The return value for _printf function is the number of characters printed or -1 if an error ocurred.

.SH AUTHORS
Written by Jenn, Braian and Zoey.
March 31, 2023
~              


